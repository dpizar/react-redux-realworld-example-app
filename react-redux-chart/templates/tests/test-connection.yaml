apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "reactReduxChart.fullname" . }}-test-connection"
  labels:
    {{- include "reactReduxChart.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "reactReduxChart.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
