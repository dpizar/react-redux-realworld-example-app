apiVersion: v1
kind: Service
metadata:
  name: {{ include "reactReduxChart.fullname" . }}
  labels:
    {{- include "reactReduxChart.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "reactReduxChart.selectorLabels" . | nindent 4 }}
